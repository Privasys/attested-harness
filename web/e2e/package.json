{
  "name": "attested-harness-e2e",
  "private": true,
  "description": "Post-deploy smoke tests for the attested-harness browser surface",
  "scripts": {
    "test": "playwright test",
    "install-browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
